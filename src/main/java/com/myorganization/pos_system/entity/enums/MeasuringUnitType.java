package com.myorganization.pos_system.entity.enums;

import javax.persistence.Enumerated;

//We can't create a package name as enum
public enum MeasuringUnitType {
NUMBER,KILO_GRAM,MILLI_GRAM,LITTER,MILLI_LITTER
}
